node_modules/
dist/

# Env files (API keys). npm login tokens stay in ~/.npmrc — never in the repo.
.env
.env.*
!.env.example

# Do not commit registry auth; use user ~/.npmrc or CI secrets.
.npmrc

*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

.DS_Store
Thumbs.db

coverage/
*.tsbuildinfo
